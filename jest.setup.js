// Jest setup file for global test configuration
global.console = {
  ...console,
  // Uncomment to ignore console outputs during testing
  // log: jest.fn(),
  // debug: jest.fn(),
  // info: jest.fn(),
  // warn: jest.fn(),
  // error: jest.fn(),
};
